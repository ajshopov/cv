import React from "react";
import Typography from '@material-ui/core/Typography';

const Booklist = () => (
  <Typography variant="body1" gutterBottom>
    Below is a selection of books that have shaped my thinking. The first section is a range of self-help and work/industry related books, and the second section a collection of everything else.
  </Typography>
);

export default Booklist;